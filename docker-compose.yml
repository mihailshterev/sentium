version: "3.8"

services:
  sentium-identity:
    container_name: sentium-identity
    build:
      context: .
      dockerfile: services/IdentityProvider/IdentityProvider.Api/Dockerfile
    ports:
      - "7001:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
